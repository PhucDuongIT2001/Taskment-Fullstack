spring:
  datasource:
    url: jdbc:mysql://35.240.172.27:3306/taskment?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456@Aaa
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # Tự động tạo bảng
    show-sql: false # Tắt log SQL để đỡ nặng log
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Tắt Multipart limit vì lưu trên Cloud Run sẽ bay file, 
# Tạm thời để nguyên để lúc demo không báo lỗi
spring.servlet.multipart.max-file-size: 10MB
spring.servlet.multipart.max-request-size: 10MB
